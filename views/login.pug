html
	body
		form(action="/login/verify", method="post")
			label(for="username") Username:
			input#user(type="text", name="username")
			br
			label(for="password") Password:
			input#pass(type="password", name="password")
			br
			input#submit(type="submit", name="Login")
